server:
  port: 0  # Random port for tests

spring:
  application:
    name: homebrain-agent-test

logging:
  level:
    root: warn
    com.homebrain: debug
    com.embabel: debug
    org.wiremock: info

# Embabel Agent configuration for tests
embabel:
  models:
    default-llm: claude-sonnet-4-5

# Test configuration
app:
  engine:
    url: http://localhost:9999
  automations:
    path: /tmp/test-automations
